import React from 'react'
import ReactDOM from 'react-dom'
import { App } from './app'
const MOUNT_NODE = document.getElementById('app')

ReactDOM.render(
    <div>
        <App/>
    </div>,MOUNT_NODE
)